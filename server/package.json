{
    "name": "server",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "concurrently \"tsc -w\" \"nodemon --watch dist dist/index.js\"",
        "build": "tsc -b"
    },
    "devDependencies": {
        "@types/node": "^24.0.13",
        "concurrently": "^9.2.0",
        "nodemon": "^3.1.10"
    },
    "dependencies": {
        "@fastify/static": "^8.2.0",
        "fastify": "^5.4.0"
    }
}
